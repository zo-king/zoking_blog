# P23 C 端布局验收

## 自动检查

- Hugo Extended 0.160.1 production/minify build：通过，54 pages。
- `git diff --check`：通过。
- 首页技术文章不依赖装饰封面，无失败资源。

## 浏览器检查

| 视口 | 结果 |
|---|---|
| 1280x720 | 三栏宽度约 219 / 682 / 268px，首页文章流首屏可扫描 |
| 390x844 | 菜单和文章不重叠，无横向溢出 |
| 320x740 | 顶栏操作和长中文标题均在容器内，无横向溢出 |

额外检查：暗色按钮保持唯一 ID，GitHub/RSS 外链可访问，移动端侧栏 `top` 偏移只在桌面 sticky 断点生效。
